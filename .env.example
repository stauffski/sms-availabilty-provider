// Google Calendar
GOOGLE_CLIENT_ID='YOUR_GOOGLE_CLIENT_ID'
GOOGLE_CLIENT_SECRET='YOUR_GOOGLE_CLIENT_SECRET'
GOOGLE_REDIRECT_URI='http://localhost:3000/oauth2callback' # Or your deployed app's callback URL
GOOGLE_CALENDAR_ID='primary' # Usually 'primary' or a specific calendar ID

// Twilio
TWILIO_ACCOUNT_SID='YOUR_TWILIO_ACCOUNT_SID'
TWILIO_AUTH_TOKEN='YOUR_TWILIO_AUTH_TOKEN'
TWILIO_PHONE_NUMBER='YOUR_TWILIO_PHONE_NUMBER'

// Application
WHITELISTED_NUMBERS='+1xxxxxxxxxx,+1yyyyyyyyyy' # Comma-separated list of phone numbers in E.164 format
PORT=8080 # Port for the server to listen on

// Location (Placeholder - see README)
// Define how "home" is determined. Could be coordinates for a future geofencing implementation,
// or just a simple flag you update manually/programmatically elsewhere.
// For now, the logic will assume a simple boolean environment variable.
HARDCODED_IS_AT_HOME='true' # Set to 'true' or 'false' as a placeholder 